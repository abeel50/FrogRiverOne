def findLeaves(X,A):
    if len(A) > 0:
        p = A.find(X)
        temp_A = A[:p+1]
        cmp = [0] * X
        for i in temp_A:
            


def solution(X, A):
    # write your code in Python 3.6
    return findLeaves(X,A)